namespace GameAbilitySystem.Logic
{
    public enum EDefaultEvent
    {
        OnAddAbility,
        OnRemoveAbility,
        OnActivateAbility,
    }
    
    public enum EGameEvent
    {
       
    }
}